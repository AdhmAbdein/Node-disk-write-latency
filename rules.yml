
groups:
  - name: node_disk_write_latency
    rules:
      - record: node_disk_write_latency_rule
        expr: rate(node_disk_write_time_seconds_total[1m]) / rate(node_disk_writes_completed_total[1m])
        labels:
          node1: node_disk_write_latency

